name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello how are u"
      - name: Print Goodbye
        run: echo "Bye GoodNight"
